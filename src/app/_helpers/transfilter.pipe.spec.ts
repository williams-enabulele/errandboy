import { TransfilterPipe } from './transfilter.pipe';

describe('TransfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
