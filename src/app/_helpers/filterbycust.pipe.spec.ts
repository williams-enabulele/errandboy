import { FilterbycustPipe } from './filterbycust.pipe';

describe('FilterbycustPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycustPipe();
    expect(pipe).toBeTruthy();
  });
});
